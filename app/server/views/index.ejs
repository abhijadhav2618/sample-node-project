<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deployed Successfully!</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #f39c12, #8e44ad);
            color: white;
            text-align: center;
        }

        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        h1 {
            font-size: 3em;
            margin: 0.5em 0;
            animation: glow 1.5s infinite alternate;
        }

        p {
            font-size: 1.5em;
            margin: 0.5em 0;
        }

        .celebrate {
            margin: 2em 0;
            font-size: 2em;
            padding: 0.5em 1em;
            border: 2px solid white;
            border-radius: 25px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.5);
            transition: transform 0.3s ease, background 0.3s ease;
        }

        .celebrate:hover {
            transform: scale(1.1);
            background: rgba(255, 255, 255, 0.4);
        }

        footer {
            margin-top: 2em;
            font-size: 0.9em;
            opacity: 0.8;
        }

        @keyframes glow {
            from {
                text-shadow: 0 0 10px #fff, 0 0 20px #ff8c00, 0 0 30px #ff8c00, 0 0 40px #ff4500, 0 0 50px #ff4500, 0 0 60px #ff4500;
            }
            to {
                text-shadow: 0 0 20px #fff, 0 0 30px #ffa500, 0 0 40px #ffa500, 0 0 50px #ff6347, 0 0 60px #ff6347, 0 0 70px #ff6347;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>🎉 Deployed Successfully! 🎉</h1>
        <p>My project is live and running on port 3005!</p>
        <button class="celebrate" onclick="alert('Keep up the great work! 🎊')">Celebrate!</button>
        <footer>
            Made with ❤️ by <b>Abhi</b> and automation tools. Keep achieving great milestones!
        </footer>
    </div>
</body>

</html>
